<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Likedibo extends Model
{
      protected $table = 'Likedibo';

    public function store(Request $request)
    {
        // Validate the request...

        //    $word = new Word;

        //    $word->name = $request->name;

        //  $word->save();
    }
}
